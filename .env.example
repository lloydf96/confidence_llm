# Copy to .env and fill in. .env is git-ignored; this template is safe to commit.

# HuggingFace token — optional, avoids anonymous rate limits on the Qwen download.
HF_TOKEN=

# Optional overrides (sensible defaults live in notebooks/aws_config.py):
# CONFIDENCE_LLM_BUCKET=            # default: sagemaker-<region>-<account>
# CONFIDENCE_LLM_PREFIX=confidence-llm
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=us.amazon.nova-lite-v1:0
# GEN_BATCH_SIZE=8
# MAX_NEW_TOKENS=50

# Note: AWS credentials come from the SageMaker execution role, not from here.
